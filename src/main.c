#include "ft_ssl.h"

int main(int argc, char **argv) {
  if (argc <= 1 || argc > 3) {
    printf("usage: ft_ssl command [flag] [file/string]\n");
  } else {
    printf("Running program\n");
  }
  return 0;
}
